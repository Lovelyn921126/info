server:
  port: 20002
spring:
  application:
    name: product
eureka:
  instance:
    hostname: localhost
  client:
    #register-with-eureka: false
    #fetch-registry: false
    service-url:
      defaultZone: http://localhost:9001/eureka/
